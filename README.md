# XO Multiplayer Game
🎮 A React + Express + Socket.IO Tic Tac Toe game with offline and online play.

## Setup
- Run `npm install` in both `/client` and `/server` folders
- Start backend: `node server.js`
- Start frontend: `npm start`
